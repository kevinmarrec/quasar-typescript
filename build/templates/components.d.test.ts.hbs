import Vue from 'vue';
import {{name}} from 'some/path/{{name}}';

new Vue({
    el: '#app',
    {{#if data}}
    data: {
    {{#each data}}
        {{@key}}: {{this}}
    {{/each}}
    },
    {{/if}}
    components: {
        {{name}}
    },
    template: `<{{name}}{{#if fixtures}}{{#each fixtures}} {{@key}}={{this}} {{/each}}{{/if}}/>`
});
